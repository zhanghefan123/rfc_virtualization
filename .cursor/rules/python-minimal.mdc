---
description: Python 代码风格——精简、用库、仅必要注释
globs: **/*.py
alwaysApply: true
---

# Python 精简实现

- **最小必要代码**：不写冗余数据类、重复 helper、过度抽象；能 inline 就不拆函数。
- **优先用库**：配置用 yaml、表格用 pandas/csv、解析用 re/json 等标准能力，不重复造轮子。
- **注释克制**：只注释非显而易见的业务规则、数据来源、边界条件；不写流水账注释。
- **不写未要求的功能**：无需求不添加可选输出、对比脚本、双目录合并等。
- **匹配/规则类逻辑**：规则写清在 config 与模块 docstring；实现与规则一致，不在多处重复匹配。
